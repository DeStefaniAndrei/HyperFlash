import { Address, ChainId, TokenInfo, TokenInfoWithAmount } from "../common.types";
import { Logger } from "../common/logger";
import { PriceTokenService } from "./price.token.service";
export declare class CachePriceFeed extends PriceTokenService {
    #private;
    constructor(priceTokenService: PriceTokenService, ttl: number);
    calculateRelativeAmount(tokenIn: TokenInfoWithAmount, tokenOut: TokenInfo, nominal?: Address | undefined): Promise<bigint | undefined>;
    getNominalPrice(token: TokenInfo, nominal?: Uint8Array): Promise<bigint | undefined>;
    getPrice(chainId: ChainId, token: Uint8Array | null, context: {
        logger: Logger;
    }): Promise<number>;
}
//# sourceMappingURL=cachePriceTokenService.d.ts.map