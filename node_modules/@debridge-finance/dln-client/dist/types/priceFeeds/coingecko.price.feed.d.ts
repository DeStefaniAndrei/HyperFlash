import { Address, ChainId, GetDecimalsLambda, TokenInfo, TokenInfoWithAmount } from "../common.types";
import { Logger } from "../common/logger";
import { PriceTokenService } from "./price.token.service";
export declare class CoingeckoPriceFeed extends PriceTokenService {
    #private;
    constructor(token?: string, logger?: Logger);
    enableRelativeCalculations(defaultNominalTokens: Map<ChainId, Address>, getDecimals: GetDecimalsLambda): void;
    getNominalPrice(token: TokenInfo, nominal?: Uint8Array): Promise<bigint | undefined>;
    calculateRelativeAmount(tokenIn: TokenInfoWithAmount, tokenOut: TokenInfo, nominal?: Address | undefined): Promise<bigint | undefined>;
    getPrice(chainId: ChainId, token: Uint8Array | null, context: {
        logger: Logger;
    }): Promise<number>;
}
//# sourceMappingURL=coingecko.price.feed.d.ts.map