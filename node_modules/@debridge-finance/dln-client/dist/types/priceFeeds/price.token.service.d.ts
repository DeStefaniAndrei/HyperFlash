import { Address, ChainId, TokenInfo, TokenInfoWithAmount } from "../common.types";
import { Logger } from "../common/logger";
export declare abstract class PriceTokenService {
    abstract getPrice(chain: ChainId, token: Uint8Array | null, context: {
        logger: Logger;
    }): Promise<number>;
    abstract calculateRelativeAmount(tokenIn: TokenInfoWithAmount, tokenOut: TokenInfo, intermediary?: Address): Promise<bigint | undefined>;
    abstract getNominalPrice(token: TokenInfo, nominal?: Address): Promise<bigint | undefined>;
}
//# sourceMappingURL=price.token.service.d.ts.map