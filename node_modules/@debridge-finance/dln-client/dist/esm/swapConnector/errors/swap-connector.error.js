import { SwapConnectorErrorType } from "./swap-connector-error.type";
export class SwapConnectorError extends Error {
    constructor(swapConnector, errorType, message) {
        super(`${swapConnector} thrown ${SwapConnectorErrorType[errorType]}: ${message}`);
        this.swapConnector = swapConnector;
        this.errorType = errorType;
    }
}
//# sourceMappingURL=swap-connector.error.js.map