"use strict";
var _UniswapV2Connector_instances, _UniswapV2Connector_findTokenSwapPath;
Object.defineProperty(exports, "__esModule", { value: true });
exports.UniswapV2Connector = void 0;
const tslib_1 = require("tslib");
const viem_1 = require("viem");
const utils_1 = require("../utils");
const swap_connector_1 = require("./swap.connector");
const UniswapV2Router_1 = require("./assets/UniswapV2Router");
const swap_connector_error_1 = require("./errors/swap-connector.error");
const swap_connector_error_type_1 = require("./errors/swap-connector-error.type");
class UniswapV2Connector extends swap_connector_1.SwapConnector {
    constructor(connection, chainId, tokens, routerAddress, wethAddress, ethPlaceholderAddress = Buffer.alloc(20, 0), quoteTTL = 600n) {
        super();
        _UniswapV2Connector_instances.add(this);
        this.connection = connection;
        this.chainId = chainId;
        this.tokens = tokens;
        this.routerAddress = routerAddress;
        this.wethAddress = wethAddress;
        this.ethPlaceholderAddress = ethPlaceholderAddress;
        this.quoteTTL = quoteTTL;
    }
    getPath(tokenIn, tokenOut) {
        tokenIn = (0, utils_1.buffersAreEqual)(tokenIn, this.ethPlaceholderAddress) ? this.wethAddress : tokenIn;
        tokenOut = (0, utils_1.buffersAreEqual)(tokenIn, this.ethPlaceholderAddress) ? this.wethAddress : tokenOut;
        if ((0, utils_1.buffersAreEqual)(tokenIn, tokenOut))
            this.throwError(swap_connector_error_type_1.SwapConnectorErrorType.InputValidationError, `In and out tokens are equal`);
        const path = tslib_1.__classPrivateFieldGet(this, _UniswapV2Connector_instances, "m", _UniswapV2Connector_findTokenSwapPath).call(this, tokenIn, tokenOut);
        if (!path) {
            this.throwError(swap_connector_error_type_1.SwapConnectorErrorType.UnsupportedTokenAddress, "Path between tokens not found");
        }
        return path;
    }
    normalizeTokens(tokenIn, tokenOut) {
        const hasInputETH = (0, utils_1.buffersAreEqual)(tokenIn, this.ethPlaceholderAddress);
        const hasOutputETH = (0, utils_1.buffersAreEqual)(tokenOut, this.ethPlaceholderAddress);
        const normalizedTokens = {
            tokenIn: hasInputETH ? this.wethAddress : tokenIn,
            tokenOut: hasOutputETH ? this.wethAddress : tokenOut,
            inputETH: hasInputETH,
            outputETH: hasOutputETH,
        };
        return {
            ...normalizedTokens,
            path: this.getPath(normalizedTokens.tokenIn, normalizedTokens.tokenOut),
        };
    }
    async getTx(normalizedTokens, amountIn, minAmountOut, swapOutcomeRecipient) {
        const { timestamp } = await this.connection.eth.getBlock("latest");
        const deadline = BigInt(timestamp) + this.quoteTTL;
        const path = normalizedTokens.path.map((buffer) => (0, utils_1.tokenAddressToString)(this.chainId, buffer));
        if (normalizedTokens.inputETH) {
            return {
                to: (0, utils_1.tokenAddressToString)(this.chainId, this.routerAddress),
                data: (0, viem_1.encodeFunctionData)({
                    abi: UniswapV2Router_1.UniswapV2RouterABI,
                    functionName: "swapExactETHForTokens",
                    args: [minAmountOut, path, (0, utils_1.tokenAddressToString)(this.chainId, swapOutcomeRecipient), deadline],
                }),
                value: amountIn.toString(),
            };
        }
        else if (normalizedTokens.outputETH) {
            return {
                to: (0, utils_1.tokenAddressToString)(this.chainId, this.routerAddress),
                data: (0, viem_1.encodeFunctionData)({
                    abi: UniswapV2Router_1.UniswapV2RouterABI,
                    functionName: "swapExactTokensForETH",
                    args: [
                        amountIn,
                        minAmountOut,
                        path,
                        (0, utils_1.tokenAddressToString)(this.chainId, swapOutcomeRecipient),
                        deadline,
                    ],
                }),
                value: "0",
            };
        }
        else {
            return {
                to: (0, utils_1.tokenAddressToString)(this.chainId, this.routerAddress),
                data: (0, viem_1.encodeFunctionData)({
                    abi: UniswapV2Router_1.UniswapV2RouterABI,
                    functionName: "swapExactTokensForTokens",
                    args: [
                        amountIn,
                        minAmountOut,
                        path,
                        (0, utils_1.tokenAddressToString)(this.chainId, swapOutcomeRecipient),
                        deadline,
                    ],
                }),
                value: "0",
            };
        }
    }
    async getSwap(request, context) {
        if (!request.destReceiver)
            this.throwError(swap_connector_error_type_1.SwapConnectorErrorType.InputValidationError, `destReceiver must be explicitly set`);
        if (request.chainId !== this.chainId)
            this.throwError(swap_connector_error_type_1.SwapConnectorErrorType.UnsupportedChain, `Unsupported`);
        const normalizedTokens = this.normalizeTokens(request.fromTokenAddress, request.toTokenAddress);
        const estimation = await this.getEstimate(request, context);
        const amountOutMin = (estimation.amountOut * (10000n - BigInt(request.slippageBps))) / 10000n;
        return {
            amountIn: request.amountIn,
            amountOut: estimation.amountOut,
            fromToken: request.fromTokenAddress,
            toToken: request.toTokenAddress,
            rawConnectorResponse: null,
            engine: "evm",
            tx: await this.getTx(normalizedTokens, request.amountIn, amountOutMin, request.destReceiver),
        };
    }
    async getEstimate(request, context) {
        if (request.chainId !== this.chainId)
            this.throwError(swap_connector_error_type_1.SwapConnectorErrorType.UnsupportedChain, "Unsupported");
        const normalizedTokens = this.normalizeTokens(request.fromTokenAddress, request.toTokenAddress);
        // uint[] memory quote = MoraSwapV2Router(NEON_POOL).getAmountsOut(reserveAmount, path);
        const rawAmountsOut = await this.connection.eth.call({
            to: (0, utils_1.tokenAddressToString)(this.chainId, this.routerAddress),
            data: (0, viem_1.encodeFunctionData)({
                abi: UniswapV2Router_1.UniswapV2RouterABI,
                functionName: "getAmountsOut",
                args: [
                    request.amountIn,
                    normalizedTokens.path.map((address) => (0, utils_1.tokenAddressToString)(this.chainId, address)),
                ],
            }),
        });
        const amountsOut = (0, viem_1.decodeFunctionResult)({
            abi: UniswapV2Router_1.UniswapV2RouterABI,
            functionName: "getAmountsOut",
            data: rawAmountsOut,
        });
        const lastAmount = amountsOut.at(-1);
        if (!lastAmount)
            this.throwError(swap_connector_error_type_1.SwapConnectorErrorType.InternalError, `Fatal error: getAmountsOut returned nothing`);
        return {
            chainId: request.chainId,
            fromTokenAddress: request.fromTokenAddress,
            toTokenAddress: request.toTokenAddress,
            amountIn: request.amountIn,
            amountOut: lastAmount,
        };
    }
    getSupportedChains() {
        return [this.chainId];
    }
    get connectorName() {
        return `${UniswapV2Connector.name}@${this.chainId}`;
    }
    throwError(type, message) {
        throw new swap_connector_error_1.SwapConnectorError(`${this.connectorName}`, type, message);
    }
}
exports.UniswapV2Connector = UniswapV2Connector;
_UniswapV2Connector_instances = new WeakSet(), _UniswapV2Connector_findTokenSwapPath = function _UniswapV2Connector_findTokenSwapPath(startToken, endToken) {
    // Build adjacency list (graph)
    const graph = {};
    for (const [token1, token2] of this.tokens) {
        const [tokenId1, tokenId2] = [token1.toString(), token2.toString()];
        if (!graph[tokenId1])
            graph[tokenId1] = new Set();
        if (!graph[tokenId2])
            graph[tokenId2] = new Set();
        graph[tokenId1].add(token2);
        graph[tokenId2].add(token1);
    }
    // DFS to find path
    const visited = new Set();
    const path = [];
    function dfs(currentToken) {
        // If we found the end token, return true
        if ((0, utils_1.buffersAreEqual)(currentToken, endToken)) {
            path.push(currentToken);
            return true;
        }
        visited.add(currentToken);
        path.push(currentToken);
        // Explore neighbors
        for (const neighbor of graph[currentToken.toString()] || []) {
            if (!visited.has(neighbor)) {
                if (dfs(neighbor)) {
                    return true;
                }
            }
        }
        // If no path found, backtrack
        path.pop();
        return false;
    }
    // Start DFS from the startToken
    if (dfs(startToken)) {
        return path;
    }
    // If no path found, return null
    return null;
};
//# sourceMappingURL=uniswapv2.connector.js.map